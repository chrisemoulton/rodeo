defaultType: postgresql
types:
  -
    name: python2
    label: Python 2
    knownConfigurationOptions:
      host: string
      port: number
      database: string
      username: string
      password: string
    defaults:
      host: localhost
      port: 5432
      database: postgres
      username: postgres
      password: mysecretpassword
